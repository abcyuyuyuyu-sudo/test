# Core
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0
h5py>=3.6.0
pyyaml>=6.0
joblib>=1.1.0

# CLI & Utilities
click>=8.0.0
tqdm>=4.60.0
loguru>=0.5.0

# Viz (Optional for Reports)
matplotlib>=3.4.0
seaborn>=0.11.0

# Storage (Recommended)
# Needed for pandas read/write parquet (otherwise scripts fall back to CSV/PKL).
pyarrow>=12.0.0
fastparquet>=0.8.0

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0
